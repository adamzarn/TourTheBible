# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'WalkThroughTheBible' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for WalkThroughTheBible

    pod 'BTNavigationDropdownMenu'

  target 'WalkThroughTheBibleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WalkThroughTheBibleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
